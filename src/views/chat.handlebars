<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Socket.io | Chat</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/styles.css">
    <!-- Load the Socket.io Client library -->
    <script src="/socket.io/socket.io.js"></script>
</head>
<body>
    <h1>Socket.io Client</h1>
    <div id="container">
        <h2>
            Hello, <span id="user">{{user}}</span>
            <a href="/">Go back</a>
        </h2>
        <h3 id="chatName">{{chatName}}</h3>
        <div class="form-control" id="messages"></div>
        <form id="messageForm">
            <input class="form-control" type="text" name="newMessage" placeholder="Type your message here..." id="newMessage">        
            <button class="btn btn-primary">Send</button>
        </form>
    </div>
    <script src="/chat.js"></script>
</body>
</html>
